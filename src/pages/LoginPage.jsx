import { Fragment } from "react/cjs/react.development";

const LoginPage = (props) => {
  return (
    <Fragment>
      <header></header>
      <footer></footer>
    </Fragment>
  );
};

export default LoginPage;
