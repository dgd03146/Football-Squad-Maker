import React from "react";
import styles from "./footer.module.css";

const Footer = (props) => (
  <footer className={styles.footer}>
    <h3 className={styles.title}>Make your own squad</h3>
  </footer>
);

export default Footer;
